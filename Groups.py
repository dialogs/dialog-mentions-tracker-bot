class Group:
    def __init__(self, peer, user_ids, title, shortname, invite_url):
        self.peer = peer
        self.user_ids = user_ids
        self.title = title
        self.shortname = shortname
        self.invite_url = invite_url
